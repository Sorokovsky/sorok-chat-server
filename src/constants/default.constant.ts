import { join } from "node:path";

export const DEFAULT_PORT: number = 5000;
export const DEFAULT_AVATAR_PATH: string = join("images", "avatar.png");
export const DEFAULT_CHANNEL_IMAGE_PATH: string = join("images", "channel.png");
export const API_PREFIX: string = "api";
export const SWAGGER_PREFIX: string = "swagger";
export const VERSION: string = "1.0";
